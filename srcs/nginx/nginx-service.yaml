apiVersion: v1
kind: Service
metadata:
  name: nginx-svc-lb
spec:
  ports:
    - name: http-port
      port: 80
      targetPort: 80

    - name: https-port
      port: 443
      targetPort: 443
  selector:
    app: nginx-service
  type: LoadBalancer